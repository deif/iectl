name: Publish to WinGet
on:
  workflow_dispatch:
jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: vedantmgoyal9/winget-releaser@main
        with:
          identifier: DEIF.iectl
          max-versions-to-keep: 15 
          token: ${{ secrets.WINGET_TOKEN }}
